Flask==3.0.3
gunicorn==22.0.0
requests==2.32.3
rdkit==2025.3.6
# Kaggle CLI 2.2.x is the current supported series in 2026. The orchestrator
# exercises kernels push/status/output/delete, so keep the major/minor line
# explicit rather than accepting an unbounded upgrade.
kaggle==2.2.3
google-auth==2.32.0
# Reaction schemes are composited (structures + coefficients + arrow) with PIL.
# It arrives transitively with rdkit, but this file is the one that pins it.
pillow==11.3.0
